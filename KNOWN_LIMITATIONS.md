# Known Limitations — v1.1.0

- Live browser verification could not run because the browser runtime failed to initialize.
- Runtime console cleanliness and interaction behavior are not certified.
- Responsive viewport and dark-mode checks require a live browser pass.
- Export controls without existing handlers remain presentation-only.