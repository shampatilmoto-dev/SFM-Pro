# Known Limitations

- Browser runtime verification was unavailable because browser initialization failed with a sandbox helper error.
- Runtime console cleanliness is not certified.
- Responsive, theme, navigation, CRUD, and export interactions require a live browser pass before a fully certified release.
- Export controls without existing handlers remain presentation-only; no new export logic was introduced.