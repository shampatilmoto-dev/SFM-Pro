{
  "schemaVersion": "1.0.0",
  "description": "Application-level metadata contract for SFM PRO Enterprise.",
  "purpose": "Reserve a reviewed location for future application metadata without binding the current runtime.",
  "sampleObject": {
    "application": {
      "displayName": "SFM PRO Enterprise",
      "releaseLabel": "v5.0 Production Release",
      "runtimeBinding": "not-configured"
    }
  },
  "futureExtensionNotes": [
    "Add approved environment metadata only after architecture review.",
    "Do not store credentials, business data, or runtime calculations here."
  ]
}

