{
  "schemaVersion": "1.0.0",
  "description": "Widget metadata contract for future reusable dashboard elements.",
  "purpose": "Reserve neutral widget descriptors without introducing widgets.",
  "sampleObject": {
    "widget": {
      "id": "future-widget",
      "displayKey": "future.widget.label",
      "bindingState": "not-configured"
    }
  },
  "futureExtensionNotes": [
    "Future widget bindings require data-access and accessibility review.",
    "Do not place calculations, chart data, or business data in this file."
  ]
}
