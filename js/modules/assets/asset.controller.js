"use strict";
import { AssetManager } from "./asset.manager.js";
const money = value => new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value||0);
function render(){const records=AssetManager.getAll(),totals=AssetManager.totals(); document.querySelector("#assetCount").textContent=totals.count; document.querySelector("#assetCurrentValue").textContent=money(totals.currentValue); document.querySelector("#assetMarketValue").textContent=money(totals.marketValue); document.querySelector("#assetDepreciation").textContent=money(totals.depreciation); const q=document.querySelector("#assetSearch").value.toLowerCase(); const filtered=records.filter(x=>`${x.name} ${x.category} ${x.location}`.toLowerCase().includes(q)); document.querySelector("#assetTableBody").innerHTML=filtered.length?filtered.map(x=>`<tr><td><strong>${escapeHtml(x.name)}</strong><br><small class="muted">${x.id.slice(0,8).toUpperCase()}</small></td><td>${escapeHtml(x.category)}</td><td>${money(x.purchaseValue)}</td><td>${money(x.currentValue)}</td><td>${escapeHtml(x.location)||"-"}</td><td><span class="tag">${warrantyStatus(x)}</span></td><td><button class="btn" data-delete="${x.id}">Delete</button></td></tr>`).join(""):`<tr><td colspan="7" class="empty">No assets match this view.</td></tr>`;}
function escapeHtml(v){const d=document.createElement("div");d.textContent=String(v??"");return d.innerHTML;} function warrantyStatus(x){if(!x.warrantyUntil)return "Not recorded";return new Date(x.warrantyUntil)>=new Date()?"Covered":"Expired";}
document.querySelector("#assetForm").addEventListener("submit",e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{AssetManager.save({...Object.fromEntries(f),invoiceName:document.querySelector("#assetInvoice").files[0]?.name,photoName:document.querySelector("#assetPhoto").files[0]?.name});e.currentTarget.reset();document.querySelector("#assetMessage").textContent="Asset saved successfully.";render();}catch(error){document.querySelector("#assetMessage").textContent=error.message;}}); document.querySelector("#assetSearch").addEventListener("input",render); document.querySelector("#assetTableBody").addEventListener("click",e=>{const id=e.target.dataset.delete;if(id&&confirm("Delete this asset record?")){AssetManager.delete(id);render();}}); render();
